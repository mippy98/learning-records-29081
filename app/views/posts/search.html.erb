<%= render partial: "header", locals: { post: @posts } %>

<div class="contents">
  <h1>検索結果</h1>

  <ul class='post-lists'>
  <% @posts.each do |post| %>
    <li class='list'>
      <h3 class='post-title'>
          <%= post.title %>
      </h3>
    <%= link_to post_path(post.id), method: :get do %>
      <div class='post-img-content'>
        <%= image_tag post.images[0], class: "post-img" %>
      </div>
    <% end %>
    </li>
  <% end %>
</div>

<%= render partial: "footer", locals: { post: @posts } %>